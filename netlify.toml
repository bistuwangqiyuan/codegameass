[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # AUTH_SECRET — 在 Netlify 环境变量中手动设置（≥32 字符随机字符串）
  # NETLIFY_DATABASE_URL / NETLIFY_DB_URL — Neon 扩展自动注入
